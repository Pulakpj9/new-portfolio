# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store
.env
# Supabase CLI local state only — migrations, seeds, docs, and lib/supabase
# client code MUST be committed or Vercel builds break (Linux clone
# lacks whatever is ignored here, while Windows builds pass locally).
docs